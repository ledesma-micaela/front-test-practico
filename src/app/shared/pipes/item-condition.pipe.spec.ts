import { ItemConditionPipe } from './item-condition.pipe';

describe('ItemConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
